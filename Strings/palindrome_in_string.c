/* 
  Is a palindrome 
  
*/

# include <stdio.h>

int main(){
    char array[256];
    scanf("%s",array);
   bool status=find_palindrome(array)
    printf("status is %s",status ? "true":"false");
    return 0;
}

bool find_palindrome(char * array){

    int from_first;
    int from_last;
    int mid=strlen(array)
    printf("The size of the array is %d",array);
    /*for(from_first=0,from_last=0;from_first<mid+1 && from_last>mid-1;from_first++,from_last--){

    }*/
    


}
